
@model Entities.Employees


@{

    ViewBag.Title = "Marcajes";
    Layout = "~/Views/Shared/_mainLayout.cshtml";
}

@section scripts {
    <script src="~/onlinejs/jquery-3.6.0.min.js"></script>
    <script src="~/onlinejs/jquery.dataTables.min.js"></script>
    <script src="~/onlinejs/dataTables.bootstrap4.min.js"></script>
    <script src="~/onlinejs/dataTables.buttons.min.js"></script>
    <script src="~/onlinejs/buttons.html5.min.js"></script>
    <script src="~/onlinejs/buttons.bootstrap4.min.js"></script>
    <script src="~/onlinejs/dataTables.select.min.js"></script>
    <script src="~/onlinejs/bootstrap.bundle.min.js"></script>
    <script src="~/onlinejs/bootstrap.min.js"></script>
    <script src="~/onlinejs/loadingoverlay.min.js"></script>
    <script src="~/onlinejs/dataTables.responsive.min.js"></script>
    <script src="~/Content/plugin/sweetalert2/sweetalert2.all.js"></script>
    <script src="~/onlinejs/moment.min.js"></script>
    <script src="~/onlinejs/jszip.min.js"></script>
    <script src="~/onlinejs/jquery-ui.js"></script>
    <script src="~/onlinejs/spin.min.js"></script>
    <script>
        var tabla_persona;
        var popupTable;
        const maxRetries = 3;
        let retries = 0;

        function fetchData(url) {
            return new Promise(function (resolve, reject) {
                const timeout = 10000; // 10 segundos
                const controller = new AbortController();
                const signal = controller.signal;

                const timeoutId = setTimeout(() => {
                    controller.abort();
                }, timeout);

                fetch(url, { signal })
                    .then(response => {
                        clearTimeout(timeoutId);
                        if (response.ok) {
                            resolve(response.json());
                        } else {
                            reject(new Error(`Request failed with status: ${response.status}`));
                        }
                    })
                    .catch(error => {
                        clearTimeout(timeoutId);
                        if (retries < maxRetries) {
                            retries++;
                            setTimeout(() => {
                                fetchData(url).then(resolve).catch(reject);
                            }, timeout); // Espera 1 segundo antes de reintentar
                        } else {
                            reject(error);
                        }
                    });
            });
        }
        $(document).ready(function () {

            @* "ajax": {
                //"url": `/Expenses/RegisterDetailPartialjson?personId=${@Model.Id_HRMS}`,

                //"url": "@Url.Action("RegisterDetailPartialjson", "Expenses", new { personId = Model.Id_HRMS })",
                "url": "http://172.26.54.66/hcmapiws/api/viatico/GetExpenseDetailViewByPersonId?personId=" + @Model.EmployeeNumber,

                "type": "GET",
                    "dataType": "json"
            },*@
           //      $.LoadingOverlay("show");
            var tabla_persona = $('#listTable').DataTable({
                "ajax": {
                    "url": "@Url.Action("RegisterDetailPartialjson", "ExtraTime", new { personId = Model.EmployeeNumber })",
                    "type": "GET",
                    "datatype": "json"
                },
                "columns": [


                    { "data": "earnedDate" },
                    { "data": "startTime" },
                    { "data": "stopTime" },
                    { "data": "comment" },


                ],
                "dom": 'Blfrtip', // Incluye el componente de selección de registros por página


                "lengthMenu": [10, 20, 50, 100], // Opciones de selección de registros por página
                "pageLength": 10, // Número de registros por página por defecto


                "language": {
                    "url": "@Url.Content("~/Scripts/datatable.es-ES.json")"
                }, "order": [[0, 'desc']],
                responsive: true,



            });

      // $.LoadingOverlay("hide");

             });






    </script>
}
<style type="text/css">

    .buttonsContainer {
        max-width: 300px;
        margin: 0 0 0 auto;
        text-align: right;
        padding-top: 12px;
    }

    .submitButton {
        margin-bottom: 12px;
        background: steelblue;
        color: white;
    }

    .cancelButton {
        margin-bottom: 12px;
        margin-left: 8px;
    }


    .groupCaption {
        margin-top: -6px;
    }

    .clearPaddings {
        padding-top: 0;
        padding-bottom: 0;
    }
    /*.button * {
            float:right;
        }*/
</style>

@{
    string url = Url.Action("EmployeesListM", "Extratime");
    string url2 = Url.Action("Index", "Home");
}
<div style="display: inline;">
    <a href="@url2" style="color: blue; text-decoration: underline;">Home</a>
    <span style="color: blue;">\</span>
    <a href=@url style="color: blue; text-decoration: underline;">Lista empleado</a>
    <span style="color: silver;">\</span>
    <a href="javascript:void(0);" onclick="location.reload();" style="color: silver; text-decoration: underline;">
        Viatico
    </a>
</div>


<div id="content-wrapper" class="d-flex flex-column">
    <div id="content">
        <div class="container-fluid">

            <!-- Page Heading -->
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="m-0 font-weight-bold text-primary" style="color: #800 !important;">
                            Lista de viatico
                            @Html.DisplayFor(model => model.Names)
                            @Html.DisplayFor(model => model.LastNames)
                        </h5>

                    </div>
                </div>

                <div class="card-body p-3">

                    <table id="listTable" style="width:100%"
                           class="table table-striped  table-bordered table-hover display responsive nowrap">
                        <thead>
                            <tr>
                                <th>Fecha</th>
                                <th>Entrada</th>
                                <th>Salida</th>
                                <th>Tipo</th>


                            </tr>
                        </thead>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>

